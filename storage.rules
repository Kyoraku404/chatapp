rules_version = '2';

// RUSH media now lives in private Cloudflare R2. Firebase Storage is disabled.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
